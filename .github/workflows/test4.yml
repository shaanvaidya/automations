name: Test Workflow 4
on:
  schedule:
    # 20:00 UTC = 1pm PDT (summer) / 12pm PST (winter, no DST auto-adjust on GitHub Actions cron)
    - cron: "0 20 * * 1"
  workflow_dispatch: {}

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
